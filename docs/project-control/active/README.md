# Active task packets

Place only currently active task packets and evidence ledgers here. Use the task ID in
the filename, keep the packet under the schema budget, and move durable closure evidence
to `../archive/` or the matching knowledge record. Do not store secrets or full logs.

# Incident index

Search `index.yaml` by symptom first. Incidents cover competing animations, hidden-row
churn, parallel hubs, stale Skin Shortcuts, nested parameters, empty Continue Watching,
provider/skin confusion, ADB/VPN blocking, CRLF checksums, Kodi repository layout,
empty release secrets, vendor version parsing, and Mad Titan’s missing certificate.

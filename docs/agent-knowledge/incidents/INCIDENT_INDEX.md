# Incident index

Search `index.yaml` by symptom first. Incidents cover competing animations, hidden-row
churn, parallel hubs, stale Skin Shortcuts, nested parameters, empty Continue Watching,
provider/skin confusion, ADB/VPN blocking, CRLF checksums, Kodi repository layout,
empty release secrets, vendor version parsing, Mad Titan's missing certificate, and
stale provider artwork retained by Kodi's texture cache. It also records the bootstrap
repository-readiness race that left skin dependencies and Umbrella unavailable, and
the Android 9 Kodi-profile preparation/parser compatibility boundary. Provider-overlay
integration and Bootstrap ordering failures are recorded in
`INC-019-provider-overlay-bootstrap-order.md`.
